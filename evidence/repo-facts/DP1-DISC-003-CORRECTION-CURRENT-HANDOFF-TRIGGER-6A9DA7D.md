# DP1 DISC-003 Correction — CURRENT_HANDOFF generation trigger

Status: ASSUMPTION

Repository: `vfi64/agentic-project-kit`

Validation ref: `6a9da7d363ae3f97f347b79a2679f6f848d8cdf3`

Inspection date: 2026-07-15

Fact family: `DISC-003`

Related records:

- `DP1-DISC-002-READER-GRAPH-6A9DA7D.md`
- `DP1-DISC-003-WRITER-GRAPH-6A9DA7D.md`
- `DP1-DISC-004-AUTHORITY-INPUTS-6A9DA7D.md`

## Maintainer clarification

The maintainer states that `docs/handoff/CURRENT_HANDOFF.md` is generated by an Agentic Kit command on explicit user request before a chat switch.

This clarification changes the interpretation of the observed historical-leading content:

- `CURRENT_HANDOFF.md` MUST NOT be described as merely a passively accumulated or unmanaged document.
- The relevant discovery question is whether the user-triggered generation command derives the current region from authoritative inputs while preserving, ordering or accumulating historical material correctly.
- Any stale-leading-content risk, if confirmed, belongs to the generation inputs, ordering, projection boundary or preservation policy rather than to an absence of a governed generation event.

## Evidence currently established at the validation ref

The inspected repository code verifies that `agentic-kit transfer chat-switch-complete` is an explicit user-invoked pre-chat-switch command that creates a deterministic successor handoff package and updates canonical chat-switch prompt projections.

The bounded inspection has not yet located the exact code path that writes `docs/handoff/CURRENT_HANDOFF.md` itself. Therefore the maintainer clarification is recorded here as `ASSUMPTION` until that exact command, symbol and write target are evidenced at the validation ref.

## Required verification

Locate and record:

1. the exact Agentic Kit qualified command invoked by the user;
2. the command registration site;
3. the function that renders or assembles `CURRENT_HANDOFF.md`;
4. the function that writes or replaces its bytes;
5. the machine-readable and historical inputs consumed;
6. whether the operation is whole-file generation, bounded-region replacement or another governed form.

After those items are evidenced, this correction may be upgraded to `VERIFIED`, and the DISC-003 writer graph MUST be amended accordingly.

## Governance note

This record corrects interpretation only. It does not select a DPA document form, judge the current generator, or authorize migration.
